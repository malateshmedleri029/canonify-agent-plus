"""Canonify Agent+ — governed, multi-agent data-canonicalization platform.

The core engine is standard-library only so it runs offline (laptop, CI, GitHub Copilot).
GCP power-ups (Gemini, Vertex AI Feature Store, BigQuery) are optional and behind interfaces.
"""

__version__ = "0.1.0"
